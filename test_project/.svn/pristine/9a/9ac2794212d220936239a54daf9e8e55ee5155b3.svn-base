package com.study.start.service;

import java.util.List;
import java.util.Map;

import com.study.start.model.BoardVO;
import com.study.start.model.Paging;

public interface BoardService {
	List selectBoardList();
	int writeService(BoardVO boardVo);
	int delete(List<Integer> list);
	List search(Map map); 
	Paging paging(Map<String, String> map);
}
